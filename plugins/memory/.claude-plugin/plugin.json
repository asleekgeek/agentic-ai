{
  "name": "memory",
  "description": "Persistent memory for Claude Code — TS port of Cortex. Remembers across sessions automatically. Thermodynamic heat/decay model, intent-aware retrieval, biological plasticity, codebase intelligence integration. SQLite by default; PostgreSQL + pgvector when DATABASE_URL is set. 41 published papers as sources. ADR-0042 complete: MCP server now honours DATABASE_URL. 0.0.13: rule engine (add_rule, get_rules, create_trigger) now works on both backends — LSP-7 closed.",
  "version": "0.0.14",
  "author": {
    "name": "Clement Deust",
    "email": "admin@ai-architect.tools"
  },
  "homepage": "https://github.com/cdeust/agentic-ai",
  "repository": "https://github.com/cdeust/agentic-ai",
  "license": "MIT",
  "keywords": [
    "memory",
    "persistent",
    "mcp",
    "claude-code",
    "neuroscience",
    "cognitive-profiling",
    "knowledge-graph"
  ],
  "mcpServers": {
    "memory": {
      "command": "bash",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/scripts/launch.sh",
        "${CLAUDE_PLUGIN_ROOT}"
      ],
      "env": {
        "DATABASE_URL": "postgresql://localhost:5432/cortex_agentic",
        "CORTEX_MEMORY_AP_ENABLED": "1"
      }
    }
  }
}
