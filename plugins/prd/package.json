{
  "name": "@agentic/plugin-prd",
  "version": "0.0.6",
  "private": true,
  "description": "Self-contained PRD MCP server plugin. The bundle contains all workspace code; ajv is declared here because schema-oracle uses createRequire (CodeGen path) that esbuild cannot pre-bundle.",
  "type": "module",
  "main": "./dist/index.js",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
