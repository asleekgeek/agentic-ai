# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo
target/                    # Rust build dir (codebase-rust)

# Worktrees — local-only, never committed
worktrees/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
!.vscode/extensions.json   # share recommended extensions

# Test artifacts
coverage/
.vitest-cache/
parity-oracle/runs/        # parity test run outputs (large, regenerated)

# Local state
.env
.env.local
*.local

# Claude Code session state (per-developer, not shared)
.claude/session-cache.json
.claude/scheduled_tasks.lock

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Cortex local DB (developer-specific)
.cortex.db
.prd-gen/
