{
  "name": "@agentic/memory",
  "version": "0.1.0",
  "private": true,
  "description": "Cortex memory subsystem: import, recall, remember, consolidation. Pure TS port of mcp_server/.",
  "type": "module",
  "exports": {
    "./import": {
      "types": "./dist/import/index.d.ts",
      "import": "./dist/import/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
